<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    body { font-family: Arial, sans-serif; margin: 40px; line-height: 1.6; }
    .header { text-align: center; margin-bottom: 30px; border-bottom: 2px solid #333; padding-bottom: 20px; }
    .section { margin-bottom: 25px; padding: 15px; background: #f9f9f9; border-radius: 5px; }
    .user-info { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
    .vital-card { border: 1px solid #ddd; padding: 15px; margin-bottom: 15px; border-radius: 5px; }
    table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
    th { background: #f0f0f0; }
  </style>
</head>
<body>
  <div class="header">
    <h1><%= title %></h1>
    <p><strong>Report Date:</strong> <%= date %></p>
  </div>

  <div class="section">
    <h2>User Information</h2>
    <div class="user-info">
      <p><strong>Name:</strong> <%= user.first_name %> <%= user.last_name %></p>
      <p><strong>Date of Birth:</strong> <%= user.dob %></p>
      <p><strong>Gender:</strong> <%= user.gender %></p>
      <p><strong>Height:</strong> <%= user.height_cm %> cm</p>
      <p><strong>Weight:</strong> <%= user.weight_kg %> kg</p>
    </div>
  </div>

  <% if (vitals && vitals.length) { %>
  <div class="section">
    <h2>Vital Signs</h2>
    <% vitals.forEach(vital => { %>
      <div class="vital-card">
        <h3>Date: <%= vital.date %></h3>
        <table>
          <tr><th>Parameter</th><th>Value</th></tr>
          <tr><td>Heart Rate</td><td><%= vital.heart_rate || 'N/A' %> bpm</td></tr>
          <tr><td>Blood Pressure</td><td><%= vital.blood_pressure_systolic || 'N/A' %>/<%= vital.blood_pressure_diastolic || 'N/A' %> mmHg</td></tr>
          <tr><td>Weekly Activity</td><td><%= vital.weekly_activity_minutes || 'N/A' %> minutes</td></tr>
          <tr><td>BMI</td><td><%= vital.body_mass_index || 'N/A' %></td></tr>
        </table>
      </div>
    <% }) %>
  </div>
  <% } %>
</body>
</html> -->